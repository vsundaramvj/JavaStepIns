package com.excercise.HackersRank;
import java.util.Scanner;
//day 2:
public class Solution2 {
	
    public static void main(String[] args) {
        int i = 4;
        double d = 4.0;
        String s = "HackerRank ";
        Scanner scan = new Scanner(System.in);

        int inI = Integer.parseInt(scan.nextLine());
        double inD = Double.parseDouble(scan.nextLine());
        String inS = scan.nextLine();
        
        scan.close();

        System.out.println(i+inI);
        System.out.println(d+inD);
        System.out.println(s+inS);
        /* Declare nextnextInt()second integer, double, and String variables. */

        /* Read and save an integer, double, and String to your variables.*/
        // Note: If you have trouble reading the entire String, please go back and review the Tutorial closely.

        /* Print the sum of both integer variables on a new line. */

        /* Print the sum of the double variables on a new line. */
		
        /* Concatenate and print the String variables on a new line; 
        	the 's' variable above should be printed first. */

    }
}